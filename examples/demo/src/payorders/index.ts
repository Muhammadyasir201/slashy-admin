import CreditCardIcon from '@material-ui/icons/CreditCard';
import PayOrderList from './PayOrderList';
import PayOrderCreate from './PayOrderCreate';

const resource = {
    list: PayOrderList,
    create: PayOrderCreate,
    icon: CreditCardIcon,
};

export default resource;
